{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Benchmark Upserts",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/test",
            "args": [
                "-d",
                "tmpdir",
                "-m",
                "self_test",
            ],
            "cwd": "${workspaceFolder}",
            "stopOnEntry": false,
            "internalConsoleOptions": "openOnSessionStart",
            "preLaunchTask": "build.fish"
        }
    ]
}